---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: rwwilden/cf-smoketests-dashboard

inputs:
- name: resource-git-smoketests-dashboard

run:
  path: resource-git-smoketests-dashboard/ci/deploy.sh
